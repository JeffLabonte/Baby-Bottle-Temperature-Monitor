use std::fs::File;

pub fn write_to_file(file_path: String, message: String) {
    let mut file = File::create(format!(""));
}

pub fn generate_file_name_with_now_time() -> String {
    String::new()
}
